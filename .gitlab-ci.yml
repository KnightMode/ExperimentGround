image: golang:1.9

before_script:
  - mkdir -p /go/src/gitlab.com/dockerdeploytest /go/src/_/builds
  - cp -r $CI_PROJECT_DIR /go/src/gitlab.com/dockerdeploytest/src
  - ls $CI_PROJECT_DIR
  - ln -s /go/src/gitlab.com/dockerdeploytest /go/src/_/builds/dockerdeploytest
  - cd src
  - go build

stages:
  - test
  - build

test:
  stage: test
  image: golang:1.9
  script:
    - go test ./...

build:
  image: golang:1.9
  stage: build
  script:
    - go build